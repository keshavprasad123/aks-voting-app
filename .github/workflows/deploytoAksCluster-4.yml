on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    
    - uses: actions/checkout@master
    
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Create ACR
      id: create-acr
      run: |
        az acr create -n sdfewqrf3qwerqwq -g sdfewqrf3qwerqwq9e19-rg --location "East US" --sku Standard --admin-enabled
        echo "::set-output name=acr_username::`az acr credential show -n sdfewqrf3qwerqwq --query username | xargs`"
        echo "::set-output name=acr_password::`az acr credential show -n sdfewqrf3qwerqwq --query passwords[0].value | xargs`"
        echo "::add-mask::`az acr credential show -n sdfewqrf3qwerqwq --query passwords[0].value | xargs`"

    - uses: azure/docker-login@v1
      with:
        login-server: sdfewqrf3qwerqwq.azurecr.io
        username: ${{ steps.create-acr.outputs.acr_username }}
        password: ${{ steps.create-acr.outputs.acr_password }}

    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/src/analytics/1.0" -f  "src/analytics/1.0/Dockerfile" -t sdfewqrf3qwerqwq.azurecr.io/kesahptest1:${{ github.sha }} --label dockerfile-path=src/analytics/1.0/Dockerfile
        docker push sdfewqrf3qwerqwq.azurecr.io/kesahptest1:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      id: login
      with:
         kubeconfig: ${{ secrets.aks_kesahptest1_kubeConfig }}
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep kesahptest18c7e | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace kesahptest18c7e`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: kesahptest18c7e
        container-registry-url: sdfewqrf3qwerqwq.azurecr.io
        container-registry-username: ${{ steps.create-acr.outputs.acr_username }}
        container-registry-password: ${{ steps.create-acr.outputs.acr_password }}
        secret-name: kesahptest1dockerauth
       
    - uses: azure/k8s-deploy@v1.2
      with:
        namespace: kesahptest18c7e
        manifests: |
          manifests/deployment-4.yml
          manifests/service-4.yml
        images: |
          sdfewqrf3qwerqwq.azurecr.io/kesahptest1:${{ github.sha }}
        imagepullsecrets: |
          kesahptest1dockerauth   
